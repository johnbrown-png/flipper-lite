# Tier 1 Lightweight Requirements
# Ultra-minimal dependencies for CSV lookup only

streamlit>=1.28.0
pandas>=2.0.0

# That's it! No FAISS, no transformers, no OpenAI
# Perfect for lightweight deployment
